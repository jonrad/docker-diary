{
  "name": "docker-builder",
  "version": "1.0.0",
  "description": "",
  "main": "build/src/index.js",
  "scripts": {
    "start": "npm run compile && node build/src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc && npm run cp-resources ",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "cp-resources": "cp -r ./src/resources ./build/src/",
    "publish": "npm run compile && docker build . -t jonrad/dockerfile-builder:v1"
  },
  "files": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "@types/text-encoding": "0.0.35",
    "@types/uuid": "^8.3.0",
    "@types/yargs": "^15.0.10",
    "gts": "^3.0.2",
    "tslint": "^5.20.1",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "node-pty": "^0.9.0",
    "text-encoding": "^0.7.0",
    "uuid": "^8.3.1",
    "yargs": "^16.1.1"
  }
}
