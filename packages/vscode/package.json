{
  "name": "dockerfile-builder-vscode",
  "displayName": "dockerfile-builder",
  "publisher": "jonrad",
  "description": "",
  "repository": "https://github.com/jonrad/dockerfile-builder",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.47.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:dockerfile-builder.runForWorkspace",
    "onCommand:dockerfile-builder.runForFile"
  ],
  "main": "./build/extension.js",
  "contributes": {
    "configuration": [
      {
        "title": "Dockerfile Builder",
        "properties": {
          "dockerfile-builder.docker": {
            "type": "string",
            "default": "",
            "description": "Path to docker binary"
          }
        }
      }
    ],
    "commands": [
      {
        "command": "dockerfile-builder.runForWorkspace",
        "title": "Dockerfile Builder"
      },
      {
        "command": "dockerfile-builder.runForFile",
        "title": "Dockerfile Builder"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "dockerfile-builder.runForFile",
          "when": "false"
        }
      ],
      "explorer/context": [
        {
          "command": "dockerfile-builder.runForFile",
          "when": "resourceFilename == Dockerfile || resourceFilename =~ /\\.Dockerfile$/"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run build && echo hello",
    "build": "tsc -p ./ --importHelpers",
    "lint": "eslint src --ext ts",
    "watch": "tsc -watch -p ./",
    "pretest": "yarn run build && yarn run lint",
    "test": "node ./build/test/runTest.js",
    "package": "vsce package --yarn"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.11.0",
    "@types/vscode": "^1.47.0",
    "@typescript-eslint/eslint-plugin": "^2.30.0",
    "@typescript-eslint/parser": "^2.30.0",
    "eslint": "^6.8.0",
    "glob": "^7.1.6",
    "mocha": "^7.1.2",
    "vscode-test": "^1.3.0"
  },
  "dependencies": {
    "dockerfile-builder-lib": "file:../lib/dockerfile-builder-lib-v1.0.0.tgz",
    "tslib": "^2.1.0",
    "typescript": "^4.1.3"
  }
}
